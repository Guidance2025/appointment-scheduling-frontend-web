// import { SECTIONS_COURSES_URL, SECTIONS_CLUSTERS_URL, EXIT_STUDENTS_URL, EXIT_STUDENT_DETAIL_URL, EXIT_SUBMIT_URL } from "./api";

// if (!res.ok) { const msg = typeof data === "string" ? data : JSON.stringify(data); 
//     throw new Error(HTTP ${res.status} ${res.statusText}: ${msg}); } return data; };

// export const fetchClusters = async () => { 
//     return jsonFetch(SECTIONS_CLUSTERS_URL, { method: "GET", headers: buildHeaders(false) }); };

// return jsonFetch(EXIT_SUBMIT_URL, { method: "POST", headers: buildHeaders(true), 
//     body: JSON.stringify({ studentId, responses }) }); 
// };